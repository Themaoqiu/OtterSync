[project]
name = "src"
version = "0.1.0"
description = "OtterSync's backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.136.0",
    "openapi>=2.0.0",
    "sqlmodel>=0.0.38",
]

